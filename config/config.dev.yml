# Configuración de ambiente de desarrollo
# Primero se carga la configuración de producción y luego la configuración de desarrollo que la redefine
imports:
    - { resource: config.prod.yml }
    - { resource: dev/parameters.yml }
    - { resource: dev/services.yml }
